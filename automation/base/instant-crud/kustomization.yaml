apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
    - service.yaml
    - deployment.yaml

commonLabels:
    app.kubernetes.io/name: instant-crud
    app.kubernetes.io/component: nstant-crud